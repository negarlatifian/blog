/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  env: {
    USERNAME: "negarl",
    PASSWORD: "negarBlog22",
  },
};

module.exports = nextConfig;
